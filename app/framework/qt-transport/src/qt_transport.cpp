// MOC anchor — qt-transport's adapters (BridgeChannelAdapter) are header-only
// but carry Q_OBJECT. This translation unit gives the static library a real
// object file so MOC-generated symbols are reliably linked into the .lib.
