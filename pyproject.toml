[project]
name = "delightful-qt-web-shell"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "assertpy>=1.1",
    "pytest>=9.0.2",
    "pywinauto>=0.6.9",
]
